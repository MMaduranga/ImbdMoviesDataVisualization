"""
frame['actorName'].duplicated().sum()
, "Shape of Table", dataframe.shape, 'Columns of the table',
                    dataframe.columns,'Number of null values in columns', dataframe.isnull().sum(axis=0)
col1,col2 = st.columns(2)
with col2.form(key='abec'):
    st.slider('slider',0,100)
with col1.form(key='abc'):
    st.slider('slider',0,100)
    
    """
